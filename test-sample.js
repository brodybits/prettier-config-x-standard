const title = <TitleComponent style='abc'>Test Title</TitleComponent>

console.log('Hello')

const cb = x => console.log(x)

function * a () {
  yield * result1
  yield * result2
}

function b () {}

const result = condition1
  ? reduce(value1)
  : condition2
    ? reduce(value2)
    : reduce(value3)

const obj = {
  first,
  second
}
